Repository for AOKP/Funky Kernel brunching

To pull source run:
$ repo init -u https://github.com/Snuzzo/funky_aokp_manifest.git -b ics
$ repo sync

To build:
. build/envsetup.sh && brunch vigor -j"#"


